# Frontend (Vite)
# URL del backend.
#
# Dev (local):
# VITE_API_URL=http://localhost:3001
#
# Producción (recomendado): dejar vacío para usar la misma URL del frontend
# y que Vercel proxyee /api/* hacia Render vía rewrites.
VITE_API_URL=http://localhost:3001
