# Frontend — Stock Control (Sukha)

Este directorio contiene el frontend (React + Vite + Tailwind).

Para documentación completa (setup, variables de entorno, Supabase y ejecución local), ver el **README principal** del repo:

- `../README.md`

## Scripts

```bash
npm install
npm run dev
```

El frontend toma la URL del backend desde `VITE_API_URL` (ver `.env.example`).
